Text Formatter (Remove spaces)
class RemoveSpace {
    public static void main(String[] args) {
        String s="Java is easy";
        String r="";
        for(int i=0;i<s.length();i++){
            if(s.charAt(i)!=' ') r+=s.charAt(i);
        }
        System.out.println(r);
    }
}
